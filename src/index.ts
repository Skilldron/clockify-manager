import { initAxiosInstance } from "./config/client";

// Init axios instance
export const axiosInstance = initAxiosInstance();

// TODO: Fetch user informations and store informations in cache raycast api
